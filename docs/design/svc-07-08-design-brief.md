# Design Brief: SVC-07 Score Aggregator & SVC-08 Decision Engine
**For:** Claude Opus — research and implementation design  
**Repo:** XHCFS/cybersiren  
**Date:** 2026-05-03  
**Status:** Both services have stub main files; neither has real internal logic. This document provides everything needed to design and implement them.

---

## 0. How to Read This Document

This is a complete design brief. Read it top to bottom before proposing code. It contains:
- The full pipeline context (§1)
- What each service must do, exactly (§2, §3)
- The real code that already exists and must be reused (§4)
- Known open design questions that need research decisions (§5)
- Deep research prompts for the questions that are still open (§6)

Do **not** invent interfaces that contradict §4 (existing shared code). Do **not** propose a new library that conflicts with the module's existing deps. Do **not** omit the DB transaction in SVC-08 or the 30-second timeout in SVC-07.

---

## 1. Full Pipeline Context

### 1.1 Architecture Overview

CyberSiren is a Kafka-based phishing detection pipeline. Emails enter at SVC-01 and flow through 10 stages. SVC-07 and SVC-08 are the final two processing stages before notification.

```
SVC-01 (ingestion)
  → emails.raw
    SVC-02 (parser)    ← fan-out to 5 topics
      → analysis.urls       → SVC-03 (URL classifier + TI blocklist) → scores.url
      → analysis.headers    → SVC-04 (header/SPF/DKIM/DMARC rules)  → scores.header
      → analysis.attachments→ SVC-05 (attachment heuristics)        → scores.attachment
      → analysis.text       → SVC-06 (DistilBERT NLP)               → scores.nlp
      → analysis.plans      ──────────────────────────────────────────────────────┐
                                                                                   ▼
                                                              SVC-07 (aggregator) ← all 5 above
                                                                → emails.scored
                                                                  SVC-08 (decision engine)
                                                                    → emails.verdict
                                                                      SVC-09 (notification)
                                                                      SVC-10 (API/dashboard)
```

### 1.2 Technology Stack

- **Language:** Go 1.25
- **Message broker:** Redpanda (Kafka-API compatible, local dev; Apache Kafka in prod)
- **Kafka client library:** `github.com/twmb/franz-go` (NOT kafka-go, NOT sarama)
- **Database:** PostgreSQL 15 (accessed via `github.com/jackc/pgx/v5`)
- **In-memory store:** Valkey (Redis-compatible; client: `github.com/valkey-io/valkey-go`)
- **Metrics:** Prometheus (`github.com/prometheus/client_golang`)
- **Tracing:** OpenTelemetry → Jaeger (`go.opentelemetry.io/otel`)
- **Structured logging:** zerolog (`github.com/rs/zerolog`)
- **Service harness:** `shared/svckit` — handles config, logger, Kafka consumers/producers, Postgres pool, Valkey client, Prometheus server, graceful shutdown. **Both services use this.**

### 1.3 Module Path

All imports use `github.com/saif/cybersiren/...`.

### 1.4 Key Identifiers

The pipeline uses **three** identifier concepts — these are critical for SVC-08's DB writes:

| Name | Type | Meaning | Source |
|------|------|---------|--------|
| `email_id` | `int64` | Logical identifier, carries on all Kafka messages and Redis keys | Generated by SVC-01 |
| `internal_id` | `int64 BIGSERIAL` | Physical DB primary key on `emails` table | Auto-generated by Postgres on `INSERT` |
| `fetched_at` | `time.Time` | Partition key on `emails` (monthly range partitions) | Set at ingest time |

`emails.scored` carries **both** `email_id` (logical) and `(internal_id, fetched_at)` (physical DB key) so SVC-08 can write to Postgres without doing an extra lookup. This is a deliberate architectural decision in ARCH-SPEC §1.

---

## 2. SVC-07 — Score Aggregator

### 2.1 Role

SVC-07 is a **pure synchronization barrier**. It does **no scoring**. Its only job is to wait until all expected component scores for one email have arrived, then package them into a single `emails.scored` message and emit it to SVC-08.

It is **stateless** across instances: all per-email state lives in Valkey. Multiple SVC-07 instances can run in parallel; messages for the same email may arrive at different instances.

### 2.2 Inputs

Consumes **5 Kafka topics** in consumer group `cg-aggregator`:

| Topic | Producer | Shape |
|-------|----------|-------|
| `analysis.plans` | SVC-02 | `AnalysisPlan` — declares which score topics to expect |
| `scores.url` | SVC-03 | `ScoreEnvelope` (generic) |
| `scores.header` | SVC-04 | `ScoresHeaderMessage` (flat, **different shape from others**) |
| `scores.attachment` | SVC-05 | `ScoreEnvelope` (generic) |
| `scores.nlp` | SVC-06 | `ScoreEnvelope` (generic) |

**The plan message is critical.** SVC-02 publishes to `analysis.plans` simultaneously with the other topics. It contains `expected_scores: []string` — the list of score-topic names the aggregator should wait for (e.g., `["scores.url", "scores.header", "scores.nlp"]` — attachment may be absent for emails with no attachments). The aggregator must wait only for topics in this list.

### 2.3 Output

Produces to `emails.scored` (consumer group for downstream: `cg-decision-engine`).

**Full required emails.scored wire schema** (this is what SVC-08 reads):
```json
{
  "email_id": 1234567890,
  "org_id": 1,
  "internal_id": 42,
  "fetched_at": "2026-05-03T10:00:00Z",
  "url_score": 72,
  "header_score": 85,
  "attachment_score": null,
  "nlp_score": 60,
  "partial_analysis": false,
  "missing_components": [],
  "component_details": {
    "url": { /* full scores.url message */ },
    "header": { /* full scores.header message (ScoresHeaderMessage) */ },
    "attachment": null,
    "nlp": { /* full scores.nlp message */ }
  },
  "aggregation_latency_ms": 312,
  "timeout_triggered": false
}
```

**Important:** `component_details` carries the **complete score messages**, not just the score number. SVC-08 needs `component_details.header` to extract `sender_domain` for campaign fingerprinting. This is why the full message must be forwarded, not just the score value.

**Note on the current contract:** `shared/contracts/kafka/emails.go` defines `EmailsScored` with `ComponentScores map[string]float64`. This is the v0 stub shape. The real implementation needs this contract extended to carry the full schema above. **The contract change must be backward-compatible or versioned** — discuss the approach in your design.

### 2.4 Valkey State

- **Key:** `aggregator:{email_id}` (e.g., `aggregator:1234567890`)
- **Type:** Redis Hash
- **TTL:** 120 seconds from most recent write (refreshed on each `HSET`)
- **Fields:**
  - `__plan` → raw JSON of `AnalysisPlan` (set when `analysis.plans` message arrives)
  - `scores.url` → raw JSON of scores.url message
  - `scores.header` → raw JSON of scores.header message
  - `scores.attachment` → raw JSON (set only if attachment scores arrive)
  - `scores.nlp` → raw JSON
- **Completion check:** `HGETALL`, parse `__plan`, check all `expected_scores` keys are present

### 2.5 The 30-Second Timeout (MISSING FROM STUB — MUST IMPLEMENT)

The stub comment says explicitly: `// NO 30-second timeout in v0`. This **must** be implemented.

**Mechanism:** After a score arrives, check completion. If not complete, also check age:
- Compute `first_message_time` — store the `time.Now()` when the **first** message for this `email_id` arrives (use a dedicated hash field, e.g., `__started_at`).
- If elapsed since `__started_at` ≥ 30 seconds AND not all scores present → emit partial result with `partial_analysis: true`, list of `missing_components`, `timeout_triggered: true`.
- Delete the Valkey key after emitting.

**Design question:** How to trigger the timeout check. Two options:
1. **Piggyback:** On each new score arrival, check both completion AND age. If timed-out, emit partial. Simple but only fires when a message arrives (an email with only 1 score arriving and then nothing will never emit).
2. **Background sweeper:** A separate goroutine polls Redis for stale keys and emits partial results. More correct but more complex.

The spec implies option 2 is needed for correctness. Design and propose the right approach.

### 2.6 At-Least-Once Safety

The Kafka consumer is at-least-once. The current stub deletes the Valkey key **after** publishing. If the delete fails, the email will be aggregated again on the next score arrival. This is acceptable (idempotent UPSERT at SVC-08). But if the **publish succeeds** and the **delete fails**, SVC-08 will receive duplicates. Design must address this.

### 2.7 Message Order Considerations

`analysis.plans` may arrive **after** some score messages (Kafka does not guarantee cross-topic ordering). The aggregator must handle this: store whatever arrives first, check completion only once the plan is present, and re-check completion for all previously stored scores when the plan arrives.

The current stub already handles this correctly (check `all[planField]` before checking completion).

### 2.8 What the Stub Already Has (Keep These)

The existing `services/svc-07-aggregator/cmd/aggregator/main.go` (180 lines) already correctly implements:
- `flexShape` decoder that handles both `ScoresHeaderMessage` (flat int64 email_id) and `ScoreEnvelope` (nested MessageMeta)
- `HSET` + `EXPIRE` + `HGETALL` pattern
- Plan-based completion check
- Publish and DEL on completion
- `decodeScore()` that normalises scores from both shapes

**Do not throw these away.** The real implementation should promote this logic into `internal/aggregator/` and add the missing pieces on top.

### 2.9 Required Prometheus Metrics

Mirror the SVC-04 pattern (`internal/processor/metrics.go`):
- `aggregator_messages_total{topic, status}` — per-topic message counts
- `aggregator_completion_latency_ms` — histogram of time from first message to completion
- `aggregator_partial_completions_total` — counter when timeout triggers partial emit
- `aggregator_active_buckets` — gauge of how many email_ids are currently in-flight (Valkey keys)

---

## 3. SVC-08 — Decision Engine

### 3.1 Role

SVC-08 is the **terminal processing block**. It receives the aggregated score package from SVC-07 and:
1. Computes the final `risk_score` (0–100) by blending component scores
2. Applies the rule engine to adjust the score
3. Maps the score to a verdict label
4. Computes a confidence value
5. Manages campaign fingerprinting and deduplication
6. Writes everything to Postgres in a **single transaction**
7. Emits `emails.verdict` to Kafka

### 3.2 Input

Consumes `emails.scored` in consumer group `cg-decision-engine`.

### 3.3 Output

Produces to `emails.verdict` (7-day retention).

**Full required emails.verdict wire schema:**
```json
{
  "email_id": 1234567890,
  "org_id": 1,
  "verdict_label": "phishing",
  "confidence": 0.82,
  "risk_score": 78,
  "header_risk_score": 85,
  "content_risk_score": 60,
  "url_risk_score": 72,
  "attachment_risk_score": null,
  "campaign_id": 17,
  "campaign_fingerprint": "a3f8...",
  "is_new_campaign": false,
  "fired_rules": [
    {"rule_id": 42, "rule_name": "spf-fail-high-entropy-subject", "score_impact": 15}
  ],
  "verdict_source": "model",
  "model_version": "xgb-v1.2+distilbert-v1.0",
  "partial_analysis": false,
  "processing_time_total_ms": 45
}
```

### 3.4 Score Blending (Step i)

Receives `url_score`, `header_score`, `attachment_score` (nullable), `nlp_score` (nullable) from SVC-07.

SVC-08 supports two fusion methods, selected by `CYBERSIREN_DECISION__FUSION_MODE`
(or `decision.fusion_mode` in `config.yaml` — both feed `DecisionConfig` via
`shared/config`; an unrecognized value fails fast at startup). **Default:
`weighted_average`** (the v1 behaviour, unchanged). `noisy_or` is **opt-in** and runs in
**shadow** (see below) until its verdict-band recalibration lands.

#### `weighted_average` — v1 weighted mean (default)

`risk = Σ wₑ·sₑ / Σ wₑ` over **present** components, weights `url=0.35, header=0.30,
nlp=0.25, attachment=0.10` (`engine.DefaultWeights`). A missing component never shifts the
result; `partial_analysis: true` passes through.

**Known weakness it has:** it *dilutes* a confident single channel. A URL that SVC-03
**pinned to 100** (TI/blocklist hit, brand-in-subdomain guard, or L2 fusion verdict) with
clean text and header blends to only `0.35·100/(0.35+0.30+0.25) ≈ 39` → "suspicious", not
"malware" — it under-rates a *confirmed* threat. This motivates `noisy_or`.

#### `noisy_or` — probabilistic OR (opt-in, shadow)

For each **present** component `c` with score `sₑ ∈ [0,100]` and reliability `rₑ ∈ [0,1]`:

$$\text{risk} \;=\; 100\cdot\Big(1 \;-\!\!\prod_{c\,\in\,\text{present}}\!\!\big(1 - r_c\,\tfrac{s_c}{100}\big)\Big)$$

`pₑ = rₑ·sₑ/100` = P(channel *c* signals phishing); the product = P(*all* quiet); `1 −`
that = **P(at least one channel fires)**.

**Reliabilities** (`engine.DefaultReliabilities`) default to **1.0 for every channel** —
full trust, *not* tuned per channel. Each upstream score is already a fused/calibrated
signal in its own service (the URL score is SVC-03's TI+guard+L2-fused verdict, not a raw
lexical score; attachment is SVC-05's AV/TI-aware score), so the blender does not
second-guess it. Operators may lower a value if a channel proves noisy in production via
`CYBERSIREN_DECISION__RELIABILITY__{URL,HEADER,NLP,ATTACHMENT}` (or `decision.reliability.*`
in `config.yaml`) — a float in `[0,1]`; an out-of-range/non-finite value, or all-zero,
fails fast at startup (`DecisionConfig.Validate`). No channel ships at a fabricated sub-1.0
default. *(An earlier draft down-weighted URL to 0.22 based on an offline lexical-only
proxy; that incorrectly crushed the TI-confirmed production score to "benign" and was
removed.)*

**What it fixes — confirmed-signal preservation (the OR-floor):**
`risk ≥ 100·maxₑ(rₑ·sₑ/100)`, so at reliability 1.0 a channel pinned to 100 yields risk
100 (malware band) regardless of the other channels. Confirmed signals can never be diluted
below their own score, and a clean channel contributes a factor of 1 (nothing) — no
dilution. Monotone, bounded [0,100], pure arithmetic (no model / sidecar).

**Why it is opt-in, not default — two honest caveats:**
1. **Independence assumption.** The OR assumes channels are conditionally independent; real
   channels are correlated, so correlated evidence is combined optimistically and the score
   distribution sits 1–2 bands higher than the weighted mean.
2. **Bands not yet recalibrated.** The §3.6 verdict bands and rule `score_impact` values
   were tuned to the weighted-mean distribution. They must be recalibrated for the OR
   distribution **on real `emails.verdict` traffic** before this becomes the default. The
   characterization in `noisyor_blender_test.go` pins the current band map as the input to
   that work.

**Shadow.** Opt-in via `CYBERSIREN_DECISION__FUSION_SHADOW=true` (default off, so steady-state
deployments do not pay for a second blend per email). When enabled, the engine runs the
*other* fusion method through the **same pipeline the active verdict took** — nudge → rule
adjustment → reconcile — and increments `decision_fusion_shadow_disagree_total{active_band,
shadow_band}` when the two would emit different **final verdict labels**, *without* affecting
the verdict. Comparing reconciled final labels (not the raw blend band) makes the metric a
faithful measure of the distribution impact of switching before it is enabled. *(The rule
adjustment is reused from the active evaluation; a rule keyed on the pre-rule band could in
principle fire differently under the shadow score, but reusing it captures the dominant
nudge/score/reconcile effects.)*

**Evidence status.** The committed, reproducible evidence is behavioural, in
`noisyor_blender_test.go`: confirmed-signal preservation (a TI-pinned URL=100 and a
confirmed-malware attachment each reach the malware band), no-dilution, the OR-floor, and a
pinned four-band characterization. Any recall/FPR figures explored during design came from a
**synthetic** corpus with a lexical-proxy URL channel (not the production fused score); they
justify the *shape* of the fix, **not** a production accuracy claim. A real held-out
multi-channel corpus and the §3.6 band recalibration are required before `noisy_or` is
promoted to default.

#### Common

**Null components:** both blenders combine **present** components only. **Explainability:**
individual component scores are preserved in `emails.verdict` and the `emails` DB UPDATE
regardless of method. Both return per-component `Contributions`, but note the semantics
differ: the weighted blender's contributions sum to the blended score; the noisy-OR's are
per-channel probabilities `pₑ` and do **not** sum to the score.

### 3.5 Rule Engine (Step ii)

The DB contains a `rules` table. Each rule has:
- `target`: the kind of entity the rule evaluates (`'email'`, `'url'`, `'header'`, `'attachment'`)
- `logic`: JSONB blob in the same JSON-DSL already implemented in `services/svc-04-header-analysis/internal/rules/` (see §4.2)
- `score_impact`: integer −100 to +100, added to the blended score
- `status`: only `'active'` rules fire

SVC-08 loads rules with `target IN ('email', 'decision')` (SVC-04 loads `target IN ('header', 'email')`). The overlap is intentional — `target='email'` rules fire in both places.

**Cache:** Identical 3-tier pattern to SVC-04: in-memory (atomic.Value per org_id) → Valkey `rules_cache:{org_id}` → Postgres. TTL: 60 seconds. **Reuse `services/svc-04-header-analysis/internal/rules/cache.go` as the template** — this is nearly copy-pasteable with a different `Targets` list. Do not rewrite from scratch.

**Rule evaluation signal snapshot for SVC-08** (passed to the DSL evaluator):
```
"score.url"          → float64 (url_score from emails.scored)
"score.header"       → float64
"score.nlp"          → float64
"score.attachment"   → float64 (or absent if null)
"score.blended"      → float64 (the pre-rule blended score)
"partial_analysis"   → bool
"verdict.label"      → string (pre-rule score→band label, before adjustments; this is
                       the pure LabelFor band, NOT the final reconciled verdict — a
                       high-band email with no malware-grade attachment shows here as
                       "malware" even though its published verdict is "phishing")
"campaign.is_new"    → bool
"campaign.risk_score"→ float64 (rolling average from campaigns table, 0 if new)
"campaign.email_count"→ int
```

Each fired rule adds its `score_impact` to the blended score (clamped to [0, 100]).  
Each fired rule is written to `rule_hits` with `entity_type='email'`, `entity_id=internal_id`.

**The DSL evaluator already exists** — see `services/svc-04-header-analysis/internal/rules/dsl.go` and `evaluator.go`. SVC-08 can import those directly (they operate on `SignalSnapshot = map[string]any`). The evaluator returns `EvaluationResult` with `Fired []FiredRule`.

### 3.6 Verdict Thresholds (Step iii)

After score blending + rule adjustments:

| Score range | Condition | Label |
|-------------|-----------|-------|
| 0–25 | — | `benign` |
| 26–50 | — | `suspicious` |
| 51–75 | — | `phishing` |
| 76–100 | a high (malware-grade) attachment is present | `malware` |
| 76–100 | otherwise | `phishing` (high) |

> **Band ↔ fusion coupling.** These ranges (and the rule `score_impact` magnitudes that add
> to the blended score) were calibrated for the **`weighted_average`** distribution. The
> `noisy_or` blender (§3.4) shifts scores up by 1–2 bands, so these bands **must be
> recalibrated on real `emails.verdict` traffic before `noisy_or` is made the default** —
> until then it runs in shadow. `noisyor_blender_test.go` pins the current band map.

**Malware-vs-phishing reconcile (76–100 band).** The top band covers both "phishing(high)" and "malware". We disambiguate by the attachment: a top-band verdict that carries a malware-grade attachment is `malware`; a top-band verdict driven only by URL/header/NLP signals (no malware-grade attachment) is high-band `phishing` (i.e. `phishing(high)`). "High" here means the score band, not the confidence value — see the confidence note below: a `phishing(high)` verdict near a band edge can carry a confidence close to 0. This is `ReconcileLabel(score, components)`; `engine.LabelFor` is left pure (score→band only) and the reconcile wraps it.

The verdict is `malware` iff a **high, malware-grade attachment** is present:
- the attachment component is present (a `nil` component is "absent", not 0), AND
- `attachment_score >= 76` (the malware-band floor — the attachment must itself be malware-grade).

A confirmed malware-grade attachment makes the verdict `malware` **even when another component (URL/header/NLP) scored higher** — a malicious attachment is the more actionable threat, so its presence dominates the label. Only when no malware-grade attachment is present (absent, or `attachment_score < 76`) does the top-band verdict stay `phishing(high)`.

Bands 0–75 are unaffected by the reconcile — attachment is ignored there.

**Confidence is unaffected by the reconcile.** Confidence is computed against the SCORE's natural band — `Confidence(score, LabelFor(score), …)` — NOT the reconciled label. An 85 reclassified from `malware` to `phishing(high)` keeps the confidence it had in the 76–100 band; renaming the label must not move the distance-to-threshold. Passing the reconciled `phishing` label into `Confidence` would (wrongly) measure distance against the 51–75 band and collapse confidence toward 0. See §3.7.

Note: `spam` and `unknown` labels exist in the `verdict_label` enum but are not yet assigned by the automated pipeline (reserved for analyst overrides).

### 3.7 Confidence Score (Step iii, continued)

**Confidence reflects label certainty, NOT severity.** A score of 13 is just as high-confidence "benign" as a score of 98 is "malware".

**Formula from issue #96:**
```
base_confidence = distance_from_nearest_threshold / 25
```
Where `distance_from_nearest_threshold` = minimum of:
- `abs(score - lower_threshold_of_label)`
- `abs(score - upper_threshold_of_label)`

For a score of 78 (malware, range 76–100): `min(78-76, 100-78) = min(2, 22) = 2`. `base = 2/25 = 0.08`. Low confidence — barely in the malware band.

For a score of 98: `min(98-76, 100-98) = min(22, 2) = 2`. Same: `0.08`. Also low — near the edge.

For a score of 88: `min(88-76, 100-88) = min(12, 12) = 12`. `base = 12/25 = 0.48`. Moderate.

**Penalty multipliers:**
- If `partial_analysis: true`: multiply by 0.7 (missing scores reduce certainty)
- If `verdict_source == "rule"` (no ML scores, rule-only): multiply by 0.5

Clamp result to [0.0, 1.0].

**`verdict_source` logic:** If the pipeline produced full ML scores (url + header + nlp), `source = "model"`. If only rule-engine scores contributed (all ML scores were null/missing), `source = "rule"`.

### 3.8 Campaign Management (Step iv + v)

#### 3.8.1 Campaign Fingerprint

```
fingerprint = SHA256( sender_domain + "|" + url_domain + "|" + SHA256(subject_template) + "|" + intent )
```

Fields sourced from `emails.scored.component_details`:
- `sender_domain`: from `component_details.header.signals.sender_domain` OR `component_details.header` message
- `url_domain`: the **primary** URL domain from `component_details.url` (if present), else empty string
- `subject_template`: the email subject with variable content normalized (see §3.8.2)
- `intent`: from NLP output — `component_details.nlp.details["intent"]` (e.g., `"phishing"`, `"urgency"`, `""`)

**Missing fields:** If any field is unavailable (partial analysis, null component), substitute `""` (empty string). The fingerprint is still computed and stored; a partial fingerprint is better than no campaign tracking.

#### 3.8.2 Subject Template Normalization

The raw subject may contain unique tokens (order IDs, transaction IDs, dates) that would prevent matching related phishing emails. Normalize by:
1. Convert to lowercase
2. Remove leading `Re:`, `Fwd:`, `[EXTERNAL]`, and similar prefixes
3. Replace sequences of digits (≥3) with `{N}` (e.g., `ORDER-1234567` → `ORDER-{N}`)
4. Replace UUID patterns with `{UUID}`
5. Replace email addresses with `{EMAIL}`

The normalized form is **not stored** — it is used only to compute the inner `SHA256(subject_template)`, which is then concatenated into the outer fingerprint.

#### 3.8.3 UPSERT into Campaigns

```sql
INSERT INTO campaigns (fingerprint, org_id, name, threat_type, target_brand, first_seen, last_seen, risk_score, tags)
VALUES ($1, $2, $3, $4, $5, now(), now(), $6, $7)
ON CONFLICT (fingerprint) DO UPDATE SET
  last_seen  = now(),
  risk_score = ROUND((campaigns.risk_score * campaigns_email_count + EXCLUDED.risk_score) / (campaigns_email_count + 1)),
  ...
RETURNING id, (xmax = 0) AS is_new
```

The `risk_score` rolling average on campaigns is a running mean. The exact UPDATE formula needs a separate `email_count` column or the UPDATE must use a subquery — design accordingly.

Current `campaigns` table columns (from migration 001):
```
id, name, description, fingerprint, threat_type, target_brand, tags,
first_seen, last_seen, risk_score, analysis_metadata, deleted_at,
created_at, updated_at, org_id, brand_id
```
**Note:** There is no `email_count` column on campaigns. Rolling average requires knowing the current count. Use `analysis_metadata JSONB` to store `{"email_count": N}` for the running average, or propose adding the column via a new migration.

#### 3.8.4 SimHash Near-Duplicate Detection

The spec mentions `SimHash near-duplicate detection (per-org scoped via Redis)`. This is the mechanism to detect when two emails belong to the same campaign **even if their fingerprint is slightly different** (e.g., different URL domain due to typosquatting). The exact algorithm is flagged as a research question — see §6.2.

### 3.9 Campaign-Informed Scoring (Step v — research needed)

The spec says: "Campaign history (avg risk_score, email count, analyst verdicts) serves as an additional signal for future emails matching the same campaign. Specific mechanism under research."

Concretely: if `campaigns.risk_score` for the matched campaign is significantly higher than the current blended score, the campaign history should nudge the score upward. See §6.3 for the research prompt.

### 3.10 Database Writes — Single Transaction (Step vi)

All of the following happen inside **one** `pgx.TxOptions{}` transaction. Any failure rolls back everything. Kafka offset is NOT committed on failure.

**Writes (in order within the transaction):**

1. **UPDATE emails** (set score columns and campaign_id):
   ```sql
   UPDATE emails
   SET risk_score            = $3,
       header_risk_score     = $4,
       content_risk_score    = $5,
       url_risk_score        = $6,
       attachment_risk_score = $7,
       campaign_id           = $8,
       analysis_metadata     = $9
   WHERE internal_id = $1 AND fetched_at = $2
   ```
   Uses the composite PK `(internal_id, fetched_at)` from `emails.scored`. This is required for the partitioned table.

2. **INSERT INTO verdicts** (append-only, never update):
   ```sql
   INSERT INTO verdicts (entity_type, entity_id, email_fetched_at, label, confidence, source, model_version, org_id)
   VALUES ('email', $1, $2, $3, $4, $5, $6, $7)
   RETURNING id
   ```
   `entity_id = internal_id`, `email_fetched_at = fetched_at`. Both required for partitioned table joins.

3. **INSERT INTO rule_hits** (one row per fired rule):
   Reuse `db/sqlc/rules.sql.go:InsertRuleHit`. See `services/svc-04-header-analysis/internal/processor/writer.go` for the exact pattern with retry-with-backoff.

4. **UPSERT campaigns** (see §3.8.3).

5. **UPDATE emails.campaign_id** (if campaign was just created or updated, link back). This may already be covered in step 1 if campaign UPSERT RETURNING id provides the ID before the emails UPDATE.

**Transaction ordering:** Campaign UPSERT must run before emails UPDATE (need the campaign ID). Rule_hits can run after emails UPDATE.

### 3.11 sqlc Queries

The pipeline write SQL for SVC-08 lives in `db/queries/{campaigns,verdicts,emails_scores}.sql`.
`services/svc-08-decision/internal/persist/queries.go` mirrors those statements as raw `pgx`
until generated `db/sqlc` code is wired in; keep the two in sync when changing SQL.

### 3.12 Required Prometheus Metrics

- `decision_messages_total{status}` — `ok` | `error` (one increment per `emails.scored` message)
- `decision_risk_score` — histogram of final risk scores (buckets: 0,25,50,75,100)
- `decision_verdict_total{label}` — per-label verdict counts
- `decision_campaign_total{type}` — new/existing campaign
- `decision_db_write_duration_seconds` — histogram of transaction duration
- `decision_rules_fired_total{rule_id}` — per-rule counter
- `decision_processing_duration_seconds` — end-to-end per-message latency

---

## 4. Existing Code to Reuse

### 4.1 Service Harness (`shared/svckit`)

Both services bootstrap via `svckit.Run(spec)`. The harness handles:
- Config loading (`shared/config`)
- Logger init (`shared/logger`, zerolog)
- Kafka producers (one per topic in `ProducerTopics`)
- Kafka consumers (one per topic in `ConsumerTopics`, all in same `GroupID`)
- Postgres pool (when `NeedsDB: true`)
- Valkey client (when `NeedsValkey: true`)
- Prometheus metrics server
- OpenTelemetry tracing to Jaeger
- Graceful shutdown on SIGINT/SIGTERM

**SVC-07 Spec fields:**
```go
svckit.Spec{
    Name:           "svc-07-aggregator",
    NeedsValkey:    true,          // for aggregator:{email_id} hash
    NeedsDB:        false,         // aggregator has NO DB access
    ProducerTopics: []string{contracts.TopicEmailsScored},
    ConsumerTopics: []string{
        contracts.TopicAnalysisPlans,
        contracts.TopicScoresURL,
        contracts.TopicScoresHeader,
        contracts.TopicScoresAttachment,
        contracts.TopicScoresNLP,
    },
    GroupID: contracts.GroupAggregator,
}
```

**SVC-08 Spec fields:**
```go
svckit.Spec{
    Name:           "svc-08-decision",
    NeedsDB:        true,
    NeedsValkey:    true,          // for rules cache + simhash
    ProducerTopics: []string{contracts.TopicEmailsVerdict},
    ConsumerTopics: []string{contracts.TopicEmailsScored},
    GroupID:        contracts.GroupDecisionEngine,
}
```

### 4.2 Rules Package (`svc-04-header-analysis/internal/rules/`)

The following types and functions can be imported **directly** by SVC-08:

| File | What to reuse |
|------|--------------|
| `cache.go` | `Cache`, `NewCache`, `CacheConfig`, `CachedRule` — copy the whole file into `svc-08/internal/rules/cache.go` and change `Targets` from `{"header","email"}` to `{"email","decision"}`. Metric names need prefixes changed from `header_` to `decision_`. |
| `evaluator.go` | `Evaluator`, `NewEvaluator`, `EvaluationResult`, `FiredRule`, `SignalSnapshot` — import directly or copy. The evaluator is pure logic with no svc-04 dependencies. |
| `dsl.go` | The JSON-DSL interpreter — used by the evaluator. Import directly. |
| `types.go` | `CachedRule` struct definition. |

**Important:** SVC-04's rules cache metric names start with `header_rule_cache_`. SVC-08's must use a different prefix (e.g., `decision_rule_cache_`) to avoid Prometheus registration conflicts.

### 4.3 Rule Hits Writer (`svc-04-header-analysis/internal/processor/writer.go`)

The `RuleHitWriter` with retry-with-backoff can be copied verbatim. It uses `db/sqlc:InsertRuleHit` which already exists. Copy the file into `svc-08/internal/processor/writer.go` — the logic is identical.

### 4.4 Kafka Contracts (`shared/contracts/kafka/`)

```go
contracts.AnalysisPlan      // what SVC-07 reads from analysis.plans
contracts.ScoreEnvelope     // what SVC-03/05/06 emit
contracts.ScoresHeaderMessage // what SVC-04 emits (flat shape, different from generic)
contracts.EmailsScored      // what SVC-07 emits (needs extension — see §2.3)
contracts.EmailsVerdict     // what SVC-08 emits (needs extension — see §3.3)
contracts.MessageMeta       // envelope embedded in generic messages
contracts.NewMeta(emailID, orgID) // constructor
```

### 4.5 DB Package (`db/sqlc/`)

```go
dbsqlc.New(tx)              // create queries bound to a transaction
dbsqlc.InsertRuleHit(ctx, params)         // already exists
dbsqlc.InsertRuleHitParams{...}           // already exists
dbsqlc.ListActiveRulesForTargets(ctx, p)  // already exists
dbsqlc.EntityTypeEnumEmail               // constant for rule_hits inserts
```

Needs to be added (write `.sql` files + run `sqlc generate`):
- `UpdateEmailScores`
- `InsertVerdict`
- `UpsertCampaign`

### 4.6 SVC-04 Processor (`svc-04-header-analysis/internal/processor/processor.go`)

Study this as the canonical pattern for a decision-service processor. Key patterns:
- Malformed message → return nil (commit offset, log error, increment metric, do not redeliver)
- Rules load failure → publish neutral score (do not block downstream), return nil
- DB write failure → return error (do NOT commit offset; message redelivered after restart/rebalance)
- Publish failure → return error
- Structured log with `email_id`, `org_id`, `duration_ms` on every message
- OpenTelemetry span wrapping the full `Handle()` call

---

## 5. Open Design Questions

These questions are flagged in the spec as "under research" or have trade-offs that require deliberate choices. Answers from research in §6 should inform the design.

### Q1 — Score Blending Method — ✅ RESOLVED (see §3.4)
**Decision:** the **weighted average stays the default**; a **probabilistic OR (`noisy_or`)**
is added opt-in/shadow to fix the weighted mean's one real defect — it *dilutes* a
confident single channel and so under-rates a *confirmed* threat (a TI-pinned URL=100 with
clean text/header → "suspicious", not "malware"). The OR has a single-channel floor that
preserves such signals. Reliabilities default to **1.0 (full trust)**, not per-channel
tuned — an earlier draft down-weighted URL based on an offline lexical proxy and was
rejected for crushing the TI-confirmed production score. A learnable meta-classifier
(gradient boosting) was also prototyped and **rejected** (overfit synthetic data;
false-positived on lexical-noise links). `noisy_or` is **not** default because it assumes
channel independence and shifts the §3.6 bands up — those bands need recalibration on real
verdict traffic first. Selector: `CYBERSIREN_DECISION__FUSION_MODE`.

### Q2 — SimHash for Campaign Near-Deduplication
**Status:** Spec says "SimHash near-duplicate detection (per-org scoped via Redis)."  
**Question:** What exact fields feed the SimHash? Subject + sender_domain + URL domains? What Hamming distance threshold constitutes "same campaign"? How is the SimHash stored and queried in Redis?

### Q3 — Campaign-Informed Score Adjustment
**Status:** Spec says "specific mechanism under research."  
**Question:** Given that a matched campaign has historical `risk_score=85` and the current email scores `risk_score=40`, by how much should the campaign history pull the score upward? Is it a hard override, a weighted nudge, or an additional rule?

### Q4 — emails.scored Contract Extension
**Status:** The current `EmailsScored` Go struct does not carry `component_details` or `partial_analysis`.  
**Decision needed:** Extend the existing struct in-place (backward-incompatible), add new fields as `json:"...,omitempty"` (backward-compatible), or version the contract?

### Q5 — Rolling Average Storage for Campaigns
**Status:** `campaigns.risk_score` needs a rolling average but there is no `email_count` column.  
**Decision needed:** Add migration for `email_count` column, or store `{email_count: N}` in `campaigns.analysis_metadata JSONB`?

---

## 6. Deep Research Prompts

Use these as literal web search queries (Google Scholar, arXiv, Semantic Scholar) or as prompts to research tools.

---

### 6.1 Score Blending / Ensemble Combination Methods — ✅ RESOLVED

Findings that drove the §3.4 / Q1 decision:
- **The OR family avoids dilution.** A weighted mean dilutes a confident channel and
  provably cannot catch both single-channel threat types at threshold ≥ 50; a probabilistic
  OR has a single-channel floor that preserves a confirmed signal (e.g. a TI-pinned URL).
- **Per-channel reliability is *not* derivable here.** We have no real multi-channel
  labelled corpus, so per-channel reliabilities cannot be honestly *measured*. Each upstream
  score is already fused/calibrated in its own service, so the OR defaults to full trust
  (1.0); operators tune downward only on observed production noise. (An offline lexical URL
  proxy made a high URL look unreliable; that proxy is not the production signal, so
  down-weighting URL was wrong.)
- **A learnable meta-classifier is *not* appropriate yet.** A gradient-boosted stacker was
  prototyped; it overfit the synthetic corpus and false-positived on lexical-noise links.
  All recall/FPR figures here are on **synthetic** data with a lexical-proxy URL channel —
  they justify the *shape* of the fix, not a production accuracy claim. A learned layer (and
  default-on for `noisy_or`) becomes appropriate once a *real* full-email multi-channel
  corpus exists.

**Outcome:** static, config-selectable structural fusion — `weighted_average` default,
`noisy_or` opt-in/shadow pending band recalibration on real traffic. See §3.4 and
`internal/engine/noisyor_blender.go`.

---

### 6.2 SimHash for Near-Duplicate Detection in Email Campaign Clustering

**Search prompt 1 (core algorithm):**
> "SimHash algorithm near-duplicate detection implementation text fingerprint Hamming distance threshold"

**Search prompt 2 (email-specific):**
> "email campaign clustering near-duplicate detection SimHash locality sensitive hashing phishing kit fingerprinting"

**Search prompt 3 (Redis storage patterns):**
> "SimHash Redis bitfield storage near-duplicate lookup distributed cache Hamming distance"

**Search prompt 4 (alternatives):**
> "MinHash vs SimHash document similarity streaming deduplication Jaccard similarity vs cosine similarity email subject clustering"

**What to find:**
- The exact algorithm for computing a 64-bit SimHash from text tokens (shingles? word n-grams? character n-grams?)
- Appropriate Hamming distance threshold for email subject similarity (typical threshold is 3 bits out of 64 for near-duplicates)
- How to store and query SimHashes in Redis efficiently (BITCOUNT, BITPOS, or custom comparison)
- Whether SimHash on subject+body vs SimHash on just subject is better for phishing campaign detection
- Existing Go libraries: `github.com/dgryski/go-simhash` or equivalent

**Decision this informs:** §3.8.4 SimHash implementation, what text fields to hash, threshold, Redis storage key schema.

---

### 6.3 Campaign-Informed Score Adjustment / Reputation Propagation

**Search prompt 1 (email security):**
> "phishing campaign reputation scoring historical signal email classification posterior update Bayesian"

**Search prompt 2 (general threat intel):**
> "threat intelligence campaign reputation propagation score adjustment new email same campaign prior evidence"

**Search prompt 3 (practical systems):**
> "spam campaign clustering risk score propagation Gmail Yahoo threat detection published paper OR blog"

**What to find:**
- Whether campaign history should be a hard signal (if campaign risk_score ≥ X, force verdict) or a soft signal (nudge blended score by some fraction of campaign delta)
- Whether the number of emails in a campaign should affect the weight given to campaign history (more emails = higher confidence in campaign average)
- Whether analyst overrides in `verdicts` for past emails in the same campaign should propagate to future emails automatically

**Decision this informs:** §3.9 campaign-informed scoring — whether to implement a hard override, a weighted nudge, or a Bayesian posterior update.

---

### 6.4 Confidence Calibration for Threshold-Based Classifiers

**Search prompt 1:**
> "confidence score calibration threshold-based classifier distance from decision boundary label certainty vs severity"

**Search prompt 2:**
> "intrusion detection system confidence scoring partial analysis degraded mode explainability SOC analyst"

**What to find:**
- Whether the "distance from nearest threshold / 25" formula from issue #96 is a known pattern with a name
- Whether a sigmoid or Platt-scaling transformation produces better-calibrated confidence values for threshold-based classifiers
- How confidence is typically communicated to SOC analysts in commercial SIEM/XDR products (is a confidence of 0.08 useful to an analyst, or should it be bucketed into HIGH/MEDIUM/LOW?)

**Decision this informs:** §3.7 confidence formula validation and possible refinement.

---

### 6.5 Distributed Aggregation Barrier Patterns (for SVC-07)

**Search prompt 1:**
> "distributed aggregation barrier pattern Redis fan-in synchronization at-least-once Kafka microservices"

**Search prompt 2:**
> "stream processing gather pattern Redis hash TTL timeout partial result exactly-once vs at-least-once"

**Search prompt 3 (timeout mechanism):**
> "Redis keyspace notifications TTL expiry event-driven background sweeper fan-in barrier Go"

**What to find:**
- Whether using Redis keyspace notifications (`__keyevent@0__:expired`) to trigger the 30-second timeout is more reliable than a polling background sweeper
- Whether a TTL-only approach (Redis expires the key at 120s and this is used as the timeout signal) is sufficient, or whether an explicit `__started_at` timestamp field is more reliable
- Race conditions: what happens if two SVC-07 instances receive the completing message simultaneously (both read HGETALL showing complete, both try to publish)?

**Decision this informs:** §2.5 timeout mechanism design.

---

## 7. File Layout — What to Create

### SVC-07 target layout:
```
services/svc-07-aggregator/
├── cmd/aggregator/
│   └── main.go                     (replace stub, wire up internals)
├── internal/
│   ├── aggregator/
│   │   ├── aggregator.go           (core gather/completion logic)
│   │   ├── aggregator_test.go
│   │   ├── timeout.go              (30s sweeper or keyspace notification)
│   │   └── decoder.go              (flexShape → normalized form)
│   └── metrics/
│       └── metrics.go              (Prometheus counters/histograms)
└── configs/
    └── config.yaml                 (aggregation_ttl_seconds, timeout_seconds)
```

### SVC-08 target layout:
```
services/svc-08-decision/
├── cmd/decision/
│   └── main.go                     (replace stub, wire up internals)
├── internal/
│   ├── engine/
│   │   ├── engine.go               (orchestrator: blend → rules → verdict → campaign → DB)
│   │   ├── engine_test.go
│   │   ├── blender.go              (Blender interface + weighted_average blender)
│   │   ├── blender_test.go
│   │   ├── noisyor_blender.go      (probabilistic-OR blender — opt-in/shadow, §3.4)
│   │   ├── noisyor_blender_test.go
│   │   ├── confidence.go           (confidence formula)
│   │   └── confidence_test.go
│   ├── campaign/
│   │   ├── fingerprint.go          (SHA256 fingerprint computation)
│   │   ├── fingerprint_test.go
│   │   ├── simhash.go              (SimHash near-dedup)
│   │   └── simhash_test.go
│   ├── rules/
│   │   ├── cache.go                (copy+adapt from svc-04)
│   │   └── snapshot.go             (build SignalSnapshot for decision engine)
│   └── persist/
│       ├── writer.go               (single-transaction DB write)
│       ├── writer_test.go
│       └── rule_hits.go            (copy from svc-04 writer.go)
└── configs/
    └── config.yaml
```

### New DB queries needed:
```
db/queries/verdicts.sql      (InsertVerdict)
db/queries/campaigns.sql     (UpsertCampaign, GetCampaignByFingerprint)
db/queries/emails_scores.sql (UpdateEmailScores)
```
After writing: `cd /home/saif/cybersiren && sqlc generate`

---

## 8. Constraints and Non-Negotiables

1. **Single DB transaction** in SVC-08. No exceptions. All 4 write operations (emails UPDATE, verdicts INSERT, rule_hits INSERT, campaigns UPSERT) in one `pgx.BeginTx` / `Commit`.
2. **Kafka offset not committed** on DB write failure. Return `error` from the handler.
3. **Malformed Kafka messages** → commit offset (return nil), log error, increment error metric. Do not block the partition forever on poison pills.
4. **`verdicts` table is append-only.** Never UPDATE a row. Always INSERT.
5. **`rule_hits` table is append-only.** Same.
6. **SVC-07 has zero Postgres access.** Only Valkey.
7. **Both services must expose Prometheus metrics** and OpenTelemetry traces on the existing ports (configured by svckit).
8. **No new top-level dependencies** beyond what is already in `go.mod`. If SimHash requires a small library, add it; but do not pull in a full ML framework or ONNX runtime.
9. **The DSL evaluator from svc-04** must be reused for rule evaluation in SVC-08 — do not reimplement it.
10. **All scores passed as `int` in the verdict Kafka message**, as `float64` internally for blending (matching the existing ScoreEnvelope.Score type).
